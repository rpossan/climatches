# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  adapter:  postgresql
  host:     localhost
  encoding: unicode
  username: postgres
  password: 123456
  template: template0

development:
  <<: *default
  database: climatches_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: climatches_test

production:
  <<: *default
  database: climatches_production
